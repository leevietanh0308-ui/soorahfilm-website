import type { Metadata } from "next";
import Link from "next/link";
import { site } from "@/data/content";
export const metadata: Metadata = { title: "Giao hàng và đặt hàng" };
export default function ShippingPage() { const ordersEnabled = (process.env.NEXT_PUBLIC_ORDER_WEB_APP_URL || "").startsWith("https://script.google.com/macros/s/") && (process.env.NEXT_PUBLIC_ORDER_WEB_APP_URL || "").endsWith("/exec"); return <article className="text-page"><span className="eyebrow">GHI CHÚ / ĐƠN HÀNG</span><h1>Giao hàng <em>và đặt mua.</em></h1><h2>Cách đặt hàng</h2><p>{ordersEnabled ? "Thêm film vào giỏ, điền thông tin và bấm gửi yêu cầu. Trang xác nhận sẽ hiện sau khi đơn được ghi nhận. " : `Thêm film vào giỏ, điền thông tin và sao chép nội dung đơn. Gửi nội dung này qua Instagram ${site.instagramHandle}. `}Đơn chỉ được xác nhận sau khi SOORAH trả lời về tình trạng hàng, phí giao hàng và cách thanh toán.</p><h2>Phí vận chuyển</h2><p>Giá film chưa bao gồm phí vận chuyển. Phí thực tế phụ thuộc địa chỉ và phương thức giao; SOORAH sẽ xác nhận trước khi bạn quyết định thanh toán.</p><h2>Nhận trực tiếp</h2><p>Nhận trực tiếp chỉ thực hiện khi SOORAH xác nhận địa điểm và thời gian. Không đến địa điểm trước khi được xác nhận.</p><Link href="/product/kodak-ultramax-400" className="text-link">QUAY LẠI SẢN PHẨM ↗</Link></article>; }
